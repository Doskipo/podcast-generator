name: Docker build & smoke test

# Builds the image exactly as docker-compose.yml would, runs it with dummy
# API keys (real ones are only needed to actually run pipeline stages, not
# to boot the API and serve / and /docs), and checks both respond. See
# docs/decisions.md ("Packaging") for why one container / one image.
on:
  push:
    branches: [master]
    paths:
      - Dockerfile
      - .dockerignore
      - docker-compose.yml
      - pyproject.toml
      - uv.lock
      - src/**
      - web/**
      - .github/workflows/docker.yml
  pull_request:
    branches: [master]
    paths:
      - Dockerfile
      - .dockerignore
      - docker-compose.yml
      - pyproject.toml
      - uv.lock
      - src/**
      - web/**
      - .github/workflows/docker.yml
  workflow_dispatch: {}

jobs:
  build-and-smoke-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Build image
        run: docker build -t podcast-generator .

      - name: Report image size
        run: docker images podcast-generator

      - name: Run container with dummy API keys
        run: |
          cat > .env.ci <<'EOF'
          OPENAI_API_KEY=dummy
          ELEVENLABS_API_KEY=dummy
          EOF
          docker run -d --name podcast-verify -p 8000:8000 --env-file .env.ci podcast-generator

      - name: Wait for the app to come up
        run: |
          for i in $(seq 1 30); do
            if curl -sf http://localhost:8000/docs > /dev/null; then
              echo "up after ${i}s"
              exit 0
            fi
            sleep 1
          done
          echo "app did not come up in time" >&2
          docker logs podcast-verify
          exit 1

      - name: Check / serves the built UI
        run: |
          status=$(curl -s -o body.html -w "%{http_code}" http://localhost:8000/)
          echo "status: $status"
          test "$status" = "200"
          grep -qi "<div id=\"root\"" body.html  # Vite's SPA mount point, not an error page

      - name: Check /docs serves the API docs
        run: |
          status=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:8000/docs)
          echo "status: $status"
          test "$status" = "200"

      - name: Check a hard refresh on a SPA route serves the UI, not the API
        # Regression check for the /episodes route collision (API's
        # GET /episodes vs. the SPA's /episodes page) — see
        # docs/decisions.md ("API routes under /api"). A direct
        # navigation/hard-refresh on /episodes must hit the SPA fallback,
        # not the JSON list endpoint (now at /api/episodes).
        run: |
          status=$(curl -s -o body.html -w "%{http_code}" http://localhost:8000/episodes)
          echo "status: $status"
          test "$status" = "200"
          grep -qi "<div id=\"root\"" body.html

      - name: Check /api/episodes still serves the API, not the UI
        run: |
          status=$(curl -s -o body.json -w "%{http_code}" http://localhost:8000/api/episodes)
          echo "status: $status"
          test "$status" = "200"
          head -c1 body.json | grep -q "\["  # a JSON array, not HTML

      - name: Container logs (always, for debugging)
        if: always()
        run: docker logs podcast-verify || true

      - name: Clean up
        if: always()
        run: |
          docker stop podcast-verify || true
          docker rm podcast-verify || true
          rm -f .env.ci
