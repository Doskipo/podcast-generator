import Card from '../components/Card.jsx'

// Placeholder — metrics dashboard is tomorrow's work (GET /metrics/summary
// already exists on the backend; this page doesn't call it yet).
export default function Dashboard() {
  return (
    <Card title="Dashboard">
      <p className="text-sm text-slate-500">Coming tomorrow — episode metrics and cost tracking will live here.</p>
    </Card>
  )
}
